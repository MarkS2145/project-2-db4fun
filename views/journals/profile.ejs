<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Journal Journey</title>
</head>
<body>
    <h1><%=player.username%>'s Profile</h1>
    <P>To EDIT your profile, update the entries below; click SAVE CHANGES when done.</P>
    <!-- Show Profile -->
    <!-- Edit Profile -->
    <form action="/player/<%=player.id%>?_method=PUT" method="POST">
        name: <input type="text" name="name" value="<%=player.name%>" /><br>
        username: <input type="text" name="username" value="<%=player.username%>" /><br>
        password: <input type="text" name="password" value="<%=player.password%>" /><br>
    
        <input type="submit" name="" value="Save Changes" />
    
    </form>
    <!-- Team Seletion -->
    <h3>Team selection for your player</h3>
    <label for="team-select">Available Teams:</label>
    <select name="teams">
        <option value="">-- Please choose a Team --</option>
        <% for (let i=0; i < teams.length; i++) { %>
            <option value="<%= teams[i].id %>">
                <%= teams[i].name %>
            </option>
            <% } %>
    </select>
    <br><br>
    <!-- Pokemon Seletion -->
    <br /><br />
    <a href="/pokemon">View All Pokemon</a>

    <h3>Pokemon added by you:</h3>
    <% for (let i=0; i < player.Pokemons.length; i++){ %>
        <li>
            <%=player.Pokemons[i].name%></a>
        </li>
        <br />
        <% } %>
    
    <br>
    
    <!--  ADD DELETE FORM HERE-->
    <P>Click DELETE and confirm.</P>
    <form action="/player/<%=player.id%>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE" />
    </form>
    <P>Once deleted, you will need to SIGN IN again to re-gain access to this game.</P>

    
    
    <!-- Delete profile -->
    
</body>
</html>