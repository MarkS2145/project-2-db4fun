<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Author</title>
</head>
<body>
    <h1><%=user.username%></h1>
    <img src="<%=author.imageURL%>" alt="Display image of <%=author.name%> " width="500" height="600">
    <br>
    <h1>Available Journals</h1>
        <p>Click title to start your journey</p>

        <<ul id=#journalList>
            <% for (let i = 0; i < user.journal.length; i++ ) { %>
                <li>
                    <!-- SHOW link here  -->
                    <a href="/journal/<%=user.journal[i].id%>"><%=user.journal[i].title%></a><br>
                </li>
            <% } %>
        </ul>
</body>
</html>